services:
  - type: web
    name: marketmindai-backend
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: python download_data.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT